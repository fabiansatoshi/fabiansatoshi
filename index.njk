---
layout: layout.njk
title: Bienvenido a fabiansatoshi
showHeader: false
---

<h1>🎮 Bienvenido a fabiansatoshi</h1>
<p>Un espacio para hablar de videojuegos, anime, manga y más. También subo mis TikToks y notas personales.</p>

<div class="cards-grid">
  {% for post in collections.post %}
    <div class="card">
      <img class="thumb" src="{{ pathPrefix }}{{ post.data.thumbnail or 'static/default-thumbnail.jpg' }}" alt="Imagen del post">
      <h3><a href="{{ pathPrefix }}{{ post.url }}">{{ post.data.title }}</a></h3>
      <p>{{ post.templateContent | striptags | truncate(120, true, "…") }}</p>
    </div>
  {% endfor %}
</div>
